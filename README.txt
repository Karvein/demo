This is a test demo that is used as example . commit on 20210317.

Here are some notes of git :
git init 
git remote add origin git@github.com:Karvein/demoName.git
git add FileName
git add .
git commit -m "Here is message content."
	using'git commit' will lead to a new interface. using ':wq' to quit .
git push origin(hostName) master(BrachName)

git push false is caused by the IP address change of 'github.com'
